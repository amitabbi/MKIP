import { LightningElement,  api } from 'lwc';
//import getTasks from '@salesforce/apex/CallToActionTimelineCtrl.getCallToActions';

export default class CallToActionTimeline extends LightningElement {
    @api recordId;
    
  //  @wire(getTasks, { workOrderId: '$recordId' })
    //tasks;
    
}